import React from 'react';
import TestPaginator from '../Components/TestPaginator';

export default {
  title: 'TestPaginator',
  component: TestPaginator,
};

export const presentation = () => <TestPaginator />;
