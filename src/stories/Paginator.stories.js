import React from 'react';
import Paginator from '../Components/Paginator';

export default {
  title: 'Paginator',
  component: Paginator,
};

export function Presentation() {
  return <Paginator />;
}
